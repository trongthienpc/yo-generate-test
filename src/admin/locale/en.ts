import { Locale, LocaleTranslations } from 'adminjs';

const translations: LocaleTranslations = {
  actions: {},
  buttons: {},
  labels: {
    loginWelcome: 'yo-express-generator | Admin',
  },
  messages: {},
  properties: {},
  resources: {},
};

const locale: Locale = {
  language: 'en',
  translations,
};

export default locale;
